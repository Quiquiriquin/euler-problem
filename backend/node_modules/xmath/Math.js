"use strict";
exports.__esModule = true;
function modulo(n, m) {
    return (n >= 0)
        ? n % m
        : (m + (n % m)) % m;
}
exports.modulo = modulo;
function sign(n) {
    return (n > 0) ? 1
        : (n < 0) ? -1
            : 0;
}
exports.sign = sign;
function binSign(n) {
    return (n >= 0)
        ? 1
        : -1;
}
exports.binSign = binSign;
function solveQuad(a, b, c) {
    if (a === 0 && b !== 0)
        return [-c / b];
    var diskr = b * b - (4 * a * c);
    if (diskr < 0)
        return [];
    if (diskr == 0)
        return [-b];
    var w1 = (-b + Math.sqrt(diskr)) / (2 * a);
    var w2 = (-b - Math.sqrt(diskr)) / (2 * a);
    return [w1, w2].sort(function (a, b) { return a - b; });
}
exports.solveQuad = solveQuad;
